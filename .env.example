DISCORD_TOKEN=
CLIENT_ID=
# Optional: set for instant guild-scoped command registration during development
DEV_GUILD_ID=
# Optional: the Pikud Haoref (Home Front Command) alerts API only responds to requests
# from within Israel. If hosting outside Israel, set an Israeli HTTP(S) proxy here to
# enable /alerts. Leave empty if the bot runs inside Israel.
PIKUD_HAOREF_PROXY=
