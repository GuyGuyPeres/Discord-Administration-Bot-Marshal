services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: marshal-bot
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
